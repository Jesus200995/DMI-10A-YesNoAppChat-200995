name: app_chat_200995
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  cupertino_icons: ^1.0.2
  dio: ^5.3.0
  flutter:
    sdk: flutter
  provider: ^6.0.5

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.9.2  # Agrega la dependencia de flutter_launcher_icons

flutter:

  uses-material-design: true

  assets:
    - assets/images/logoapp.png  # Ruta correcta a tu imagen de ícono

flutter_icons:
  android: true            # Genera íconos para Android
  ios: true                # Genera íconos para iOS
  image_path: "assets/images/logoapp.png"  # Ruta al archivo de ícono
  ios_background_color: "#FFFFFF"  # Fondo blanco para iOS (opcional)
